<svg width="120" height="120" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <filter id="shadow">
      <feDropShadow dx="2" dy="2" stdDeviation="3" flood-opacity="0.4"/>
    </filter>
  </defs>

  <!-- Circle background -->
  <circle cx="60" cy="60" r="55" fill="#e74c3c" filter="url(#shadow)"/>
  <circle cx="60" cy="60" r="48" fill="#c0392b"/>

  <!-- VS text -->
  <text x="60" y="75" font-family="Arial" font-size="40" font-weight="bold" fill="#ffffff" text-anchor="middle">VS</text>
</svg>
